import { Text, View } from "react-native";

export default function TransferForm({ form, handleInputChange, navigation }) {
  return (
    <View
      style={{
        flex: 1,
        alignItems: "center",
        justifyContent: "center",
      }}
    >
      <Text>This is the TransferForm.</Text>
    </View>
  );
}
