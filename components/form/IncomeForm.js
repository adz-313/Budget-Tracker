import { Text, View } from "react-native";

export default function IncomeForm({ form, handleInputChange, navigation }) {
  return (
    <View
      style={{
        flex: 1,
        alignItems: "center",
        justifyContent: "center",
      }}
    >
      <Text>This is the IncomeForm.</Text>
    </View>
  );
}
